# PDF Analyzer Frontend

React frontend for uploading a PDF and asking questions about it.

## Run

```bash
npm install
npm run dev
```

The frontend calls the backend at `http://localhost:8000/api` by default.
